<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Commission">
    <EditText
            style="@style/BasicSearchInputText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:hint="@string/search"
            android:ems="10"
            android:id="@+id/searchBar" app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="16dp"
            android:textSize="12sp" app:layout_constraintEnd_toStartOf="@+id/button" android:layout_marginEnd="4dp"
            android:layout_marginRight="4dp"/>
    <Button
            style="@style/BasicOutlinedButton"
            android:text="@string/login"
            android:onClick="login"
            android:layout_width="wrap_content"
            android:layout_height="38dp" android:id="@+id/login" app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="16dp"
            app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="4dp"
            android:layout_marginRight="4dp"/>
    <Button
            style="@style/BasicContainedButton"
            android:text="@string/search"
            android:onClick="searchCharacter"
            android:layout_width="wrap_content"
            android:layout_height="38dp" android:id="@+id/button" app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="16dp"
            app:layout_constraintEnd_toStartOf="@+id/login" android:layout_marginEnd="4dp"
            android:layout_marginRight="4dp"/>



</androidx.constraintlayout.widget.ConstraintLayout>